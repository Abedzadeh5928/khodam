Enter file contents heredo

function run(msg, matches)
  return "تا ساعاتی دیگر هک میشود قربان🙂🙃"
  end
return {
  patterns = {
    "^هک در چه وضعیتیه ربات",
  },
  run = run
}
end
